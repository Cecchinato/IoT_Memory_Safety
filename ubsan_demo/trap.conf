# UBSan, trap mode: on undefined behavior, execute __builtin_trap().
#
# No runtime library needed, so this works on any architecture - including
# targets too small for picolibc's ubsan handlers. The cost is that all
# diagnostic information is gone: you get a fault, and a PC to resolve by hand.
CONFIG_UBSAN=y
CONFIG_UBSAN_TRAP=y
